syntax = "proto3";

package common;

// 공통 패킷 구조
message Packet {
  string userId = 1;         // 유저 ID (UUID, 16바이트)
  uint32 sequence = 2;       // 유저의 호출 수 (42억)
}

// Ping 메시지
message Ping {
  Packet packet = 1;
  int64 timestamp = 2;       // Ping 타임스탬프
}